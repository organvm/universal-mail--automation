# Core dependencies (Gmail API)
# Bounded pins: allow minor/patch drift, block silent major-version jumps.
google-api-python-client>=2.0.0,<3
google-auth-httplib2>=0.1.0,<1
google-auth-oauthlib>=0.5.0,<2

# Optional: Outlook.com support (Microsoft Graph API)
msal>=1.25.0,<2
requests>=2.28.0,<3

# Optional: YAML configuration file support
pyyaml>=6.0,<7

# Optional: Type checking (development)
# mypy>=1.0.0
# types-requests>=2.28.0
