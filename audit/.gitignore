# Triage receipts contain real sender addresses (PII). Belt-and-suspenders on top
# of the repo-root `audit/*.jsonl` rule: ignore EVERYTHING in this directory and
# any sub-directory (the root rule is single-level and would miss audit/2026/x.jsonl),
# except the documentation and this rule itself.
*
!.gitignore
!README.md
